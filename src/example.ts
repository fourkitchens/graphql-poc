// The purpose of this file and this function is simply to demonstrate
// that the build toolchains in this application are functioning correctly.
// This will be removed in a subsequent PR.
export default function example(a: number, b: number): number {
  return a + b;
}
